<UserControl x:Class="Raphael.Desktop.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Raphael.Desktop.Views"
             xmlns:views="clr-namespace:Raphael.Desktop.Views.Admin"
             xmlns:services="clr-namespace:Raphael.Desktop.Services"
             xmlns:helpers="clr-namespace:Raphael.Desktop.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Derived from the MaterialDesign style for TabControl -->
        <Style TargetType="TabControl"
      BasedOn="{StaticResource MaterialDesignTabControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Here we use TabPanel aligned left -->
                            <TabPanel x:Name="HeaderPanel"
                                   Grid.Row="0"
                                   IsItemsHost="True"
                                   Panel.ZIndex="1"
                                   Background="{TemplateBinding Background}"
                                   Margin="0"
                                   HorizontalAlignment="Left"/>

                            <!-- ContentPresenter that fills all the space -->
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                           Grid.Row="1"
                           Margin="{TemplateBinding Padding}"
                           ContentSource="SelectedContent"
                           SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Border BorderThickness="0,1,0,0"
        BorderBrush="#2196f3"
        Background="White">

        <Grid Margin="4 0 4 4">

            <TabControl  HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         HorizontalContentAlignment="Stretch"
                         VerticalContentAlignment="Stretch">

                <!--
                    F1 answers for the tab in front. Two rules make that work, and both were
                    broken before:

                    1. There is no HelpAssist.TopicId on this UserControl any more. There used to
                       be one — "desktop/admin/overview" on the root — and because the resolver
                       takes the shallowest declaration in the subtree, it shadowed everything
                       below it. That is why F1 always opened the top of Admin no matter which
                       tab was in front.

                    2. The topic goes on each view, never on its TabItem. A TabControl realises
                       only the selected tab's content, while every header exists at once, so a
                       topic on a header would answer for Employees whichever tab was open.

                    When nothing here matches, HelpService still falls back to the Admin topic
                    for the menu, so F1 never lands nowhere.
                -->
                <TabItem Header="{Binding Employees}">
                    <views:EmployeesView helpers:HelpAssist.TopicId="desktop/admin/employees" />
                </TabItem>


                <TabItem Header="{Binding Billing}">
                    <views:BillingView helpers:HelpAssist.TopicId="desktop/admin/billing" />
                </TabItem>

                <TabItem Header="{Binding Profile}">
                    <views:ProfileView helpers:HelpAssist.TopicId="desktop/admin/profile" />
                </TabItem>

                <TabItem Header="Integrators">
                    <views:IntegratorsView helpers:HelpAssist.TopicId="desktop/admin/integrators" />
                </TabItem>

                <TabItem Header="Providers">
                    <views:ProvidersView helpers:HelpAssist.TopicId="desktop/admin/providers" />
                </TabItem>

                <!-- ⚠️ No TopicId on this one on purpose. The subtree walk takes the shallowest
                     declaration it finds, so a topic here would shadow the three inside and F1
                     would always open the panel's overview instead of the sub-tab in front.
                     GoogleMapsAdminView declares one per sub-tab. -->
                <TabItem Header="{Binding Source={x:Static services:LocalizationService.Instance},
                                          Path=[gmaps.Tab]}">
                    <views:GoogleMapsAdminView />
                </TabItem>

            </TabControl>

        </Grid>

    </Border>
    
</UserControl>
