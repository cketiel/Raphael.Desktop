<UserControl x:Class="Raphael.Desktop.Views.Admin.ProvidersView" 
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:helpers="clr-namespace:Raphael.Desktop.Helpers">

    <UserControl.Resources>
        <helpers:BindingProxy x:Key="vmProxy" Data="{Binding}" />
    </UserControl.Resources>
    
    <Border BorderThickness="1" BorderBrush="#2196f3" Background="White">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Command="{Binding AddCommand}" HorizontalAlignment="Left" Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Margin="0,0,0,10">
                <materialDesign:PackIcon Kind="Add" />
            </Button>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Providers}"
                      SelectedItem="{Binding SelectedProvider}"
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                        SelectionMode="Single"
                        Style="{StaticResource CustomDataGridStyle}"
                        AlternationCount="2"
                        AlternatingRowBackground="White"                                    
                        ColumnHeaderStyle="{StaticResource CustomColumnHeaderStyle}"
                        RowStyle="{StaticResource CustomRowStyle}"
                        CellStyle="{StaticResource CustomCellStyle}"
                        SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Command="{Binding Data.EditCommand, Source={StaticResource vmProxy}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignToolButton}">
                                        <materialDesign:PackIcon Kind="Pencil" Foreground="Orange"/>
                                    </Button>
                                    <Button Command="{Binding Data.DeleteCommand, Source={StaticResource vmProxy}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignToolButton}">
                                        <materialDesign:PackIcon Kind="Delete" Foreground="Red"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Logo" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Ellipse Width="28" Height="28" Margin="0" Stroke="#2196f3" StrokeThickness="1">
                                    <Ellipse.Fill>
                                        
                                        <ImageBrush ImageSource="{Binding FullLogoUrl}" Stretch="UniformToFill"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Company" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="Auto" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>